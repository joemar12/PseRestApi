FROM alpine:latest

# Install docker client and curl once at build time
RUN apk add --no-cache curl docker-cli coreutils bash

# Install supercronic
RUN curl -L https://github.com/aptible/supercronic/releases/latest/download/supercronic-linux-amd64 \
  -o /usr/local/bin/supercronic \
  && chmod +x /usr/local/bin/supercronic

# Entrypoint script to run catch-up script first, then supercronic
COPY cron-entrypoint.sh /entrypoint.sh
RUN dos2unix /entrypoint.sh && chmod +x /entrypoint.sh

CMD ["/entrypoint.sh"]